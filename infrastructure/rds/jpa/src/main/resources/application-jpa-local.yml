spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/booksearch
    username: searcher
    password: searcher

  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    show-sql: true
